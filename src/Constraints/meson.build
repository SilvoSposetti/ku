constraints_library_name = 'Constraints'

constraints_dependency = declare_dependency(
  include_directories: include_directories('.'),
  dependencies: [
    core_dependency,
    drawing_dependency,
  ]
)

