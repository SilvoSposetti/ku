#include "Setter.h"

std::unique_ptr<Board> Setter::generate(const u_int8_t totalDigits,
                       const SetterType setterType,
                       const std::vector<std::unique_ptr<AbstractConstraint>>& constraints) {
    std::unique_ptr<Board> newBoard = std::make_unique<Board>();

    return newBoard;
}
